name: Auto Create PR

on:
  push:
    branches:
      - 'claude/**'

permissions:
  contents: read
  pull-requests: write

jobs:
  create-pr:
    uses: richardmsong/shared-workflows/.github/workflows/auto-pr.yml@main
    secrets: inherit
